// Copy this file to secrets.h (same directory) and fill in real values.
// secrets.h is gitignored at the repo root — it must never be committed.
#pragma once

#define WIFI_SSID_HOME "your-wifi-ssid"
#define WIFI_PASS_HOME "your-wifi-password"
